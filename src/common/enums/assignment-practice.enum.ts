/**
 * Phạm vi queue giao việc — lọc nhóm khi tạo ticket theo practice ITIL.
 */
export enum AssignmentPractice {
  INCIDENTS = 'incidents',
  PROBLEMS = 'problems',
  CHANGES = 'changes',
  SERVICE_REQUESTS = 'service_requests',
}
